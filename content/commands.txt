# from your repo root
#npm install

npm run dev
http://localhost:3000

npm run build   # (or whatever PRISM uses to export)

# ensure docs is the deploy folder and contains the exported site
rm -rf docs
cp -r out docs  # if PRISM outputs to 'out'
# (if PRISM outputs to something else like 'dist', copy that instead)

# IMPORTANT: prevent GitHub Pages/Jekyll from stripping _next/
touch docs/.nojekyll

git add -A
git commit -m "Updated the files Jan 18"
git push

https://zilai-wang.github.io/